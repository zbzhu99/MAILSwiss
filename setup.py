from setuptools import find_packages, setup

setup(
    name="mailswiss",
    description="Expectation maximization version of team individual imitation learning.",
    version="1.0.0",
    packages=find_packages(),
)
